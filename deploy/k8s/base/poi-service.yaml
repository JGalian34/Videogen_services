---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: poi-service
  namespace: poi-video-platform
  labels:
    app.kubernetes.io/name: poi-service
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: real-estate-poi-video-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: poi-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: poi-service
        app.kubernetes.io/component: api
    spec:
      containers:
        - name: poi-service
          image: poi-service:latest
          ports:
            - containerPort: 8001
          envFrom:
            - configMapRef:
                name: platform-config
            - secretRef:
                name: platform-secrets
          env:
            - name: DATABASE_URL
              value: "postgresql+asyncpg://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@$(POSTGRES_HOST):$(POSTGRES_PORT)/$(POSTGRES_DB_POI)"
            - name: KAFKA_TOPIC
              value: poi.events
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8001
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: poi-service
  namespace: poi-video-platform
  labels:
    app.kubernetes.io/name: poi-service
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/name: poi-service
  ports:
    - port: 8001
      targetPort: 8001
  type: ClusterIP

