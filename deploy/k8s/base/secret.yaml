apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: poi-video-platform
  labels:
    app.kubernetes.io/part-of: real-estate-poi-video-platform
type: Opaque
# ⚠️  Base64-encoded values.  In production, use Sealed Secrets / External Secrets.
# echo -n 'value' | base64
stringData:
  API_KEY: "dev-api-key"
  POSTGRES_PASSWORD: "changeme"
  RUNWAY_API_KEY: ""

