# tools/qa – QA harness dependencies
httpx>=0.27,<1.0
rich>=13.0
pydantic>=2.0,<3.0

# Testing (used by run.py orchestrator for unit tests)
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# Linting (invoked as subprocess)
ruff>=0.4
black>=24.0
mypy>=1.10
